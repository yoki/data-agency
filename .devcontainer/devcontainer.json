{
    "name": "Python 3.13",
    "build": {
        "dockerfile": "Dockerfile",
        "context": ".."
    },
    "mounts": [
        // To avoid re-installation of VS code extensions for every reload
        "type=volume,source=vscode-${localWorkspaceFolderBasename}-server,target=/root/.vscode-server",
        // GEMINI_API key
        "type=bind,source=/mnt/c/mydata/workspace/configs/secrets/data_agency,target=/secrets/data_agency,readonly",
        "type=bind,source=/mnt/c/mydata/amro-asia.org/AFSR24 - Dollar Financing - General/20_Data/new_download,target=/data_readonly,readonly"
    ],
    "postCreateCommand": "uv pip install --system -e .[jupyter,dev]",
    "privileged": true,
    "containerEnv": {
        "DATA_AGENCY_STATE": "/workspaces/${localWorkspaceFolderBasename}/data_agency_state",
        "DATA_AGENCY_DATA_ROOT": "/data_readonly"
    },
    "customizations": {
        "vscode": {
            "extensions": [
                "ms-python.python",
                "ms-toolsai.jupyter",
                "charliermarsh.ruff"
            ],
            "settings": {
                "files.eol": "\n",
                "remote.containers.forwardGpg": false, // saves time
                "remote.containers.forwardX11": false, // saves time
                "[python]": {
                    "editor.defaultFormatter": "charliermarsh.ruff",
                    "editor.formatOnSave": true,
                    "editor.codeActionsOnSave": {
                        "source.fixAll": "always",
                        "source.organizeImports": "never"
                    }
                }
            }
        }
    }
}